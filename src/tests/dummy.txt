import { mount, shallow } from 'enzyme';
import { MemoryRouter } from 'react-router-dom';
import App from '../App';

// describe('App', () => {
//   test('Nav routes to homepage at base url', () => {
//     const wrapper = mount(
//       <MemoryRouter initialEntries={['/']}>
//         <App />
//       </MemoryRouter>,
//     );
//     console.log(wrapper.debug());
//   });

//   test('Nav routes to homepage when "Home" is clicked', () => {});

//   test('Nav routes to about us page when "About"', () => {});
// });
